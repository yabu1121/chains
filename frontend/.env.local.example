# Base URL of the Chains backend API. Copy to .env.local for development, or
# set NEXT_PUBLIC_API_BASE_URL in the build/runtime environment for production.
# This is inlined into the client bundle at build time, so it must point at the
# publicly reachable API origin (https in production).
NEXT_PUBLIC_API_BASE_URL=https://api.chains.example.com
